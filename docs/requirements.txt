# Dependencies for building the documentation site, used by CI and for local
# builds. The docs build never imports scdef -- mkdocstrings reads src/ statically
# via griffe -- so the package itself and its heavy runtime deps (jax, scanpy,
# graphviz) are deliberately not installed here.
#
# Kept in step with the docs entries of the `dev` group in pyproject.toml.
mkdocs>=1.6,<2
mkdocs-material>=9.7,<10
mkdocs-jupyter>=0.25
mkdocstrings>=1.0,<2
mkdocstrings-python>=2.0,<3
mkdocs-autorefs>=1.4
