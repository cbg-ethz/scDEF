# Local profile for a 32-core / 188GB RAM / 1 GPU machine.
# GPU methods are serialized via --resources gpu=1.
# CPU methods run in parallel up to the core/memory budget.
cores: 32
resources:
  - gpu=1
  - mem_mb=188000
