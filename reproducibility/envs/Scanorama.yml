name: scanorama
channels:
  - conda-forge
  - bioconda
  - nodefaults  ## block user/system channels
dependencies:
  - python=3.10
  - leidenalg
  - pip
  - pip:
      - scanorama
      - scdef==0.1.5