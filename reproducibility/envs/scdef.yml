name: scdef
channels:
  - conda-forge
  - bioconda
  - nodefaults  ## block user/system channels
dependencies:
  - python=3.10
  - graphviz
  - leidenalg
  - pip
  - pip:
      - scdef==0.1.5
      #- jax[cuda12]
