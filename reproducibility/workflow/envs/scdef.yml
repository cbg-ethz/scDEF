name: scdef
channels:
  - conda-forge
  - bioconda
  - nodefaults  ## block user/system channels
dependencies:
  - python=3.10
  - scanpy
  - graphviz
  - leidenalg
  - pip
  - pip:
    - scdef==0.3.5
    - jax[cuda12]
    - scikit-misc 