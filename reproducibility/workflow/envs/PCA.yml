name: PCA
channels:
  - conda-forge
  - bioconda
  - nodefaults  ## block user/system channels
dependencies:
  - python=3.10
  - scanpy
  - leidenalg
  - pip
  - pip:
    - scikit-misc
    - scdef==0.3.1
